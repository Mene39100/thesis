\chapter{Ambiente sperimentale}
\label{cha:ambienteSperimentale}

\section{Obiettivo e criteri di progettazione del testbed}

L'Obiettivo di base per la progettazione del testbed è quello di creare un ambiente
controllato, riproducibile e parametrizzabile all'interno del quale poter
valutare il differente comportamento dei demoni di sincronizzazione temporale in
condizioni di rete variabili. Nel caso in questione: linuxptp, chrony e ntpsec.
L'impostazione è stata scelta per mettere in evidenza non la precisione assoluta
dei nodi rispetto a una sorgente temporale esterna, quindi con un riferimento UTC,
ma la loro capacità nel mantenere una relazione temporale coerente all'interno
dell'ambiente sperimentale, al differire delle condizioni dei canali di comunicazione.

L'ambiente si basa su tre criteri fondamentali. Per primo bisogna isolare
completamente il testbed dalla rete esterna. Non essendoci necessità di riferimenti
esterni è stato possibile disabilitare ogni forma di connettività, garantendo il
massimo isolamento e costringendo i nodi a sincronizzarsi all'interno della sola
topologia di rete. Il secondo criterio è stato la riproducibilità dell'ambiente.
Infine l'ultimo criterio coinvolgeva la parametrizzazione degli impairment di
rete, i quali devono essere facilmente modificabili, potendo gestire in modo
indipendente da ogni script la loro applicazione.

La topologia è stata creata con l'utilizzo dei container Docker. I rami logici sono
stati opportunamente separati e lo stesso ragionamento è stato applicato agli script,
con l'obiettivo di ridurre al minimo interferenze tra demoni e protocolli
differenti. Le condizioni di rete sono state modificate mediante l'utilizzo di Linux
Traffic Control.

\section{Topologia di rete}
La topologia si divide su due reti logiche sperate, rappresentative dell'evoluzione
della topologia T1, utilizzata per la fase di test preliminari, ma poi risultata
obsoleta. La rete A è stata associata a una subnet \texttt{10.0.10.0/24}, mentre la
rete B è stata associata ad una subnet \texttt{10.0.20.0/24}. I nodi \texttt{servergm},
\texttt{serverntp}, \texttt{clientchrony} e l'interfaccia \texttt{eth0} di \texttt{boundary}
sono stati collocati nella rete A, mentre \texttt{clientptp}, \texttt{clientntp}
e l'interfaccia \texttt{eth1} di\texttt{boundary} sono stati collocati nella rete
B. La scelta si basa sulla praticità operativa, si è cercato di evitare i conflitti
di configurazioni tra demoni e protocolli differenti, raccogliendo al meglio, successivamente,
i dati.

Il nodo \texttt{boundary} è stato configurato come nodo intermedio tra le due reti
assumendo un differente ruolo al variare del demone considerato. Nel caso di PTP,
assume un ruolo di boundary clock ricevendo sincronizzazione dalla rete A e propagandola
verso la rete B, mentre nel caso di NTPsec, è utilizzato come nodo intermedio tra \texttt{serverntp}
e \texttt{clientntp}. Per Chrony non è stato necessario il suo utilizzo.

{ \setlength{\intextsep}{8pt} \setlength{\textfloatsep}{8pt} \setlength{\floatsep}{8pt}

\begin{table}[H]\captionsetup{skip=4pt} \begin{tabular}{lll}\hline \textbf{Nodo} & \textbf{Rete} & \textbf{Ruolo sperimentale} \\ \hline \texttt{servergm} & A & Sorgente interna per PTP e Chrony \\ \texttt{serverntp} & A & Sorgente interna per NTPsec \\ \texttt{boundary} & A / B & Nodo intermedio; boundary clock PTP \\ \texttt{clientptp} & B & Nodo client PTP \\ \texttt{clientntp} & B & Nodo client NTPsec \\ \texttt{clientchrony} & A & Nodo client Chrony \\ \hline\end{tabular} \caption{Nodi della topologia T2 e ruoli assegnati nel testbed.} \label{tab:nodi_t2}\end{table}

\vspace{1cm}

\begin{figure}[H]\centering \begin{tikzpicture}[ node distance=1.5cm and 1.8cm, box/.style={ rectangle, draw, rounded corners, align=center, minimum width=3.8cm, minimum height=1.25cm, font=\small }, netbox/.style={ rectangle, draw, rounded corners, inner sep=0.45cm, fill=gray!8 }, arrow/.style={ -{Latex[length=3mm]}, thick }, edgeLabel/.style={ midway, fill=white, inner sep=1pt, font=\footnotesize } ]
% --- Nodi rete A ---
\node[box] (servergm) {%
\texttt{servergm}\\ Grandmaster PTP / Server Chrony\\ \texttt{eth0: 10.0.10.1} };

\node[box, right=2.2cm of servergm] (serverntp) {%
\texttt{serverntp}\\ Server NTPsec\\ \texttt{eth0: 10.0.10.4} };

\node[box, below=1.5cm of servergm] (clientchrony) {%
\texttt{clientchrony}\\ Client Chrony\\ \texttt{eth0: 10.0.10.3} };

\node[box, below=1.5cm of serverntp] (boundary) {%
\texttt{boundary}\\ Boundary Clock / nodo intermedio\\ \texttt{eth0: 10.0.10.2}\\ \texttt{eth1: 10.0.20.1} };


% --- Nodi rete B ---
\node[box, below=3.0cm of boundary, xshift=-2.2cm] (clientntp) {%
\texttt{clientntp}\\ Client NTPsec\\ \texttt{eth0: 10.0.20.2} };

\node[box, below=3.0cm of boundary, xshift=2.2cm] (clientptp) {%
\texttt{clientptp}\\ Client PTP\\ \texttt{eth0: 10.0.20.3} };


% --- Domini/reti ---
\begin{scope}[on background layer]\node[netbox, fit=(servergm)(serverntp)(clientchrony)(boundary)] (domainA) {};

\node[netbox, fit=(clientntp)(clientptp)] (domainB) {};\end{scope}


% --- Titoli domini ---
\node[font=\bfseries, above=0.15cm of domainA.north] {Rete A -- \texttt{10.0.10.0/24}};

\node[font=\bfseries, above=0.15cm of domainB.north] {Rete B -- \texttt{10.0.20.0/24}};


% --- Collegamenti logici ---
\draw[arrow] (servergm) -- node[edgeLabel] {Chrony} (clientchrony); \draw[arrow] (servergm) -- node[edgeLabel] {PTP} (boundary); \draw[arrow] (serverntp) -- node[edgeLabel] {NTPsec} (boundary); \draw[arrow] (boundary) -- node[edgeLabel, left] {NTPsec} (clientntp); \draw[arrow] (boundary) -- node[edgeLabel, right] {PTP} (clientptp);\end{tikzpicture} \caption{Topologia logica T2 utilizzata nel testbed sperimentale. La rete A ospita le sorgenti temporali interne e il client Chrony, mentre la rete B ospita i client PTP e NTPsec. Il nodo \texttt{boundary} collega le due reti tramite le interfacce \texttt{eth0} e \texttt{eth1}.} \label{fig:topologia_t2}\end{figure}

\vspace{0.4cm} }

Per ragioni configurative, si è reso necessario separare la sorgente temporale
in due nodi differenti, distinguendo tra PTP e Chrony da un lato, e NTPsec dall'altro.
Conflitti di configurazione, e permessi rendevano problematica l'esecuzione
simultanea sullo stesso nodo, soprattutto nei casi in
cui più servizi potevano tentare di intervenire sullo stesso clock locale. Inoltre
la separazione logica delle rete permette di avere una distinzione più chiara tra
le diverse catene di sincronizzazione analizzate nel testbed.

\section{Ambiente testbed}

Attraverso la creazione di una topologia di rete "containerizzata" e l'ausilio di Katharà 
è stato realizzato il testbed. Katharà è un sistema di emulazione di rete open source 
basato su container. Questa scelta ha reso possibile la
riproduzione di una rete composta da un insieme di nodi Linux isolati, ognuno dotato
di una sua interfaccia, di file di configurazioni personalizzati e degli
specifici demoni di sincronizzazione. L'utilizzo dei container ha permesso inoltre
di automatizzare l'intero ciclo sperimentale, rendendo possibile l'inizializzazione
della topologia, la configurazione degli indirizzi IP, l'installazione dei
pacchetti necessari, la copia dei file di configurazione, l'avvio dei demoni, l'applicazione
degli impairment di rete e infine la raccolta dei log.

Ogni run può essere eseguita a partire da uno stato pulito della topologia,
riducendo il rischio che processi residui, configurazioni di rete precedenti o discipline
di coda già applicate influenzino le misurazioni successive.

\subsection{Kathará e organizzazione dei container}

La topologia T2 viene descritta nel file \texttt{lab.conf}, dove vengono
definiti i nodi del testbed, le differenti reti virtuali a cui essi appartengono,
i volumi condivisi montati all'interno dei container e le capability necessarie
all'esecuzione degli esperimenti.

Nella topologia, descritta come T2, i nodi sono associati a due reti logiche
distinte, indicate come rete A e rete B. I nodi \texttt{servergm}, \texttt{serverntp},
\texttt{clientchrony} e l'interfaccia \texttt{eth0} di \texttt{boundary} appartengono
alla rete A. I nodi \texttt{clientntp}, \texttt{clientptp} e l'interfaccia \texttt{eth1}
di \texttt{boundary} appartengono alla rete B. Il nodo \texttt{boundary} è l'unico
container connesso a entrambe le reti interne e costituisce il punto di interconnessione
tra i due domini.

\begin{lstlisting}[language=bash,caption={Estratto del file \texttt{lab.conf}: assegnazione dei nodi alle reti A e B.},label={lst:labconf_topologia}]

# Topologia T2

servergm[0]=A
serverntp[0]=A
boundary[0]=A
boundary[1]=B
clientchrony[0]=A
clientntp[0]=B
clientptp[0]=B
\end{lstlisting}

Nella fase iniziale di avvio, ogni container va ad eseguire il proprio file di inizializzazione
\texttt{.startup}. Essi contengono gli indirizzi IP delle interfacce, attivano i
link, popolano il file \texttt{/etc/hosts}, installano i pacchetti necessari e copiano
i file di configurazione dei demoni dalle directory condivise del progetto verso i
percorsi attesi dal sistema.

Nel nodo \texttt{boundary}, per esempio, si configurano due interfacce interne: \texttt{eth0}
sulla rete A e \texttt{eth1} sulla rete B.

\begin{lstlisting}[language=bash,caption={Estratto di \texttt{boundary.startup}: configurazione delle interfacce interne.},label={lst:boundary_startup_ip}]
ip addr add 10.0.10.2/24 dev eth0
ip addr add 10.0.20.1/24 dev eth1
ip link set eth0 up
ip link set eth1 up
\end{lstlisting}

Per quanto riguarda la risoluzione locale dei nomi, ogni nodo mantiene un file \texttt{/etc/hosts}
che associa gli hostname ai rispettivi indirizzi IP, evitando problematiche derivanti
dalla dipendenza da servizi DNS esterni e rende esplicita la corrispondenza tra hostname
e indirizzi IP della topologia.

\begin{lstlisting}[language=bash,caption={Mappatura locale degli hostname nei container.},label={lst:hosts_mapping}]
10.0.10.1 servergm
10.0.10.4 serverntp
10.0.10.2 boundary0
10.0.20.1 boundary1
10.0.10.3 clientchrony
10.0.20.2 clientntp
10.0.20.3 clientptp
\end{lstlisting}

Distinguendo tra \texttt{boundary0} e \texttt{boundary1} si riesce a
identificare in modo chiaro le due interfacce del nodo \texttt{boundary}.
\texttt{boundary0} corrisponde all'interfaccia sulla rete A (\texttt{10.0.10.2}),
mentre \texttt{boundary1} corrisponde all'interfaccia sulla rete B (\texttt{10.0.20.1}).
Ciò è utile ai fini del test NTPsec, nei quali il nodo \texttt{clientntp}
utilizza il boundary come sorgente temporale raggiungibile sulla rete B.

All'interno del file \texttt{lab.conf} viene montata una directory del progetto all'interno
di tutti i container. Tale volume condiviso viene utilizzato sia per rendere
accessibili i file di configurazione ai nodi, sia per salvare i log prodotti durante
le run sperimentali.

\begin{lstlisting}[language=bash,caption={Estratto del file \texttt{lab.conf}: volume condiviso tra host e container.},label={lst:labconf_volume}]
servergm[volume]=/home/gabrielemenestrina/tesi_sync_lab|/tesi_sync_lab|rw
serverntp[volume]=/home/gabrielemenestrina/tesi_sync_lab|/tesi_sync_lab|rw
boundary[volume]=/home/gabrielemenestrina/tesi_sync_lab|/tesi_sync_lab|rw
clientchrony[volume]=/home/gabrielemenestrina/tesi_sync_lab|/tesi_sync_lab|rw
clientntp[volume]=/home/gabrielemenestrina/tesi_sync_lab|/tesi_sync_lab|rw
clientptp[volume]=/home/gabrielemenestrina/tesi_sync_lab|/tesi_sync_lab|rw
\end{lstlisting}

L'obiettivo alla base di tale scelta è duplice. Se da un lato permette di mantenere
tutti i file di configurazione all'interno della repository del progetto e copiarli
nei differenti container in fase di avvio, allo stesso modo consente ai demoni e
agli script di scrivere i log in una directory persistente accessibile dall'host,
evitando problematicità dovute alla chiusura della topologia.

Ad esempio, il nodo \texttt{servergm} installa Chrony e linuxptp, copia la
configurazione Chrony lato server e predispone il file di configurazione PTP da
utilizzare come grandmaster interno del dominio sperimentale.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
apt update -y
apt install -y chrony linuxptp

cp /tesi_sync_lab/configs/chrony/server_chrony.conf /etc/chrony/chrony.conf

pkill chronyd 2>/dev/null
sleep 2
chronyd &

mkdir -p /etc/ptp
cp /tesi_sync_lab/configs/ptp/server_ptp.conf /etc/ptp/server_gm.conf
\end{minted}
\caption{Estratto di \texttt{servergm.startup}: installazione e copia delle configurazioni.}
\label{lst:servergm_startup}
\end{listing}

Analogamente, il nodo \texttt{clientchrony} installa Chrony, copia la
configurazione client e avvia manualmente il demone \texttt{chronyd}. La scelta
dell'avvio manuale è stata preferita poiché, all'interno dei container, la
gestione tramite \texttt{systemctl} non è necessaria e può risultare meno adatta rispetto
all'avvio diretto del processo.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
apt update -y
apt install -y chrony

cp /tesi_sync_lab/configs/chrony/client_chrony.conf /etc/chrony/chrony.conf

pkill chronyd 2>/dev/null
sleep 2
chronyd &
\end{minted}
\caption{Estratto di \texttt{clientchrony.startup}: configurazione e avvio del client Chrony.}
\label{lst:clientchrony_startup}
\end{listing}

In NTPsec, il nodo \texttt{serverntp} installa il pacchetto \texttt{ntpsec},
copia la configurazione server e avvia \texttt{ntpd}. Una procedura simile viene
adottata per il nodo\texttt{clientntp} che però utilizza una configurazione lato
client. Per quanto riguarda invece il nodo \texttt{boundary} si ha sia la
configurazione PTP sia la configurazione NTPsec, ma i demoni non vengono mantenuti
attivi simultaneamente in quanto potrebbero essere causa di conflitti interni,
come problematicità di permessi.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
apt update -y
apt install -y ntpsec

cp /tesi_sync_lab/configs/ntp/server_ntp.conf /etc/ntpsec/ntp.conf

pkill ntpd 2>/dev/null
sleep 2
/usr/sbin/ntpd -g -c /etc/ntpsec/ntp.conf &
\end{minted}
\caption{Estratto di \texttt{serverntp.startup}: configurazione e avvio di NTPsec.}
\label{lst:serverntp_startup}
\end{listing}

Il nodo \texttt{clientptp} installa invece \texttt{linuxptp} e copia il file di configurazione
PTP lato client. L'avvio effettivo del demone \texttt{ptp4l} non avviene direttamente
nel file \texttt{.startup}, ma viene gestito successivamente dagli script di esecuzione,
in modo da controllare l'ordine di avvio del grandmaster, del boundary clock e del
client PTP.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
apt update -y
apt install -y linuxptp

mkdir -p /etc/ptp
cp /tesi_sync_lab/configs/ptp/client_ptp.conf /etc/ptp/client_ptp.conf
\end{minted}
\caption{Estratto di \texttt{clientptp.startup}: predisposizione della configurazione PTP lato client.}
\label{lst:clientptp_startup}
\end{listing}

Uno script differente, indicato come \texttt{bootstrapT3\_V2.sh}, viene utilizzato
per coordinare l'intera esecuzione sperimentale. Esso definisce gli scenari da
eseguire, l'insieme delle run, la directory di output dei log, il percorso della topologia
Kathará e l'interfaccia su cui applicare gli impairment per i test che perturbano
il ramo B.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
SCENARIOS=("low" "medium" "high")
SCENARIO_DIR="$(dirname "$0")/scenarios"
RAWLOG_ROOT="analysis/raw_logs/T3_multiplerun"
NETNS="boundary"
IFACE="eth1"   # boundary lato rete B
TOPOLOGY_DIR="topologies/T2"

RUNS=15

mkdir -p "$RAWLOG_ROOT"
\end{minted}
\caption{Variabili principali dello script \texttt{bootstrapT3\_V2.sh}.}
\label{lst:bootstrap_vars}
\end{listing}

Ogni scenario viene eseguito più volte. Nel caso mostrato, per ciascuno scenario
sono previste quindici run indipendenti. Permettendo di ridurre eventuali rumori statistici
e di ottenere una distribuzione più affidabile dei risultati.

\subsection{Privilegi e capability}

Ogni esecuzione dei demoni di sincronizzazione richiede specifici privilegi all'interno
dei container, e lo stesso accade per l'applicazione degli impairment di rete introdotti,
da qui nasce la necessità di avviare in modalità privilegiata la topologia e di
assegnare capability aggiuntive ai nodi, \texttt{--privileged}. Così facendo si sono
ridotti problemi legati alle restrizioni imposte dal runtime dei container.

Le capability sono un meccanismo del kernel Linux che consente di concedere privilegi
specifici a processi o container senza dover concedere loro l'accesso completo come
utente root. Di seguito sono riportate le capability principali utilizzate nel
testbed: \texttt{SYS\_TIME}, \texttt{NET\_RAW} e \texttt{NET\_ADMIN}.

\begin{lstlisting}[language=bash,caption={Estratto del file \texttt{lab.conf}: capability assegnate ai container.},label={lst:labconf_capabilities}]
clientptp[cap_add]=SYS_TIME,NET_RAW,NET_ADMIN
boundary[cap_add]=SYS_TIME,NET_RAW,NET_ADMIN
servergm[cap_add]=SYS_TIME,NET_RAW,NET_ADMIN
serverntp[cap_add]=SYS_TIME,NET_RAW,NET_ADMIN
clientchrony[cap_add]=SYS_TIME,NET_RAW,NET_ADMIN
\end{lstlisting}

\texttt{SYS\_TIME} consente processi nel container di intervenire sull'orologio di
sistema, questo indipendentemente se poi avviene o meno. Si è reso necessario l'aggiunta
di essa poiché era origine di conflitti internii, in quanto essa è necessaria
per i demoni che disciplinano il clock locale, come \texttt{chronyd}, \texttt{ntpd}
e \texttt{ptp4l}. La capability \texttt{NET\_ADMIN} è necessaria per operazioni amministrative
sulle interfacce di rete, tra cui l'applicazione di qdisc tramite \texttt{tc}.
La capability \texttt{NET\_RAW} abilita invece funzionalità legate all'uso di socket
raw e operazioni di rete richieste da alcuni strumenti o implementazioni.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
sudo -v
printf 'n\n' | sudo -n kathara lstart -d "$TOPOLOGY_DIR" --privileged
\end{minted}
\caption{Avvio privilegiato della topologia nello script di automazione.}
\label{lst:kathara_privileged}
\end{listing}

Nella fase di testbed la topologia viene "ripulita" e riavviata prima di ogni
scenario o run. È stato effettuato tramite \texttt{kathara lclean} eliminando i
container della run precedente, evitando che processi attivi, configurazioni
residue o discipline di coda rimangano applicate all'esecuzione successiva.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
kathara lclean -d "$TOPOLOGY_DIR" || true
sudo -v
printf 'n\n' | sudo -n kathara lstart -d "$TOPOLOGY_DIR" --privileged
\end{minted}
\caption{Reset della topologia prima dell'avvio di una run.}
\label{lst:kathara_lclean}
\end{listing}

L'uso di \texttt{|| true} impedisce che un eventuale errore nella fase di
pulizia, ad esempio dovuto all'assenza di una topologia già attiva, interrompa
lo script.

Un'ulteriore precauzione riguarda l'esecuzione dei demoni sul nodo \texttt{boundary}.
Poiché tale nodo può essere coinvolto sia nei test PTP sia nei test NTPsec, gli script
evitano di mantenere attivi contemporaneamente demoni che potrebbero tentare di disciplinare
lo stesso clock locale. Nel caso PTP, prima dell'avvio di \texttt{ptp4l} sul
grandmaster viene terminato \texttt{chronyd}; nel caso NTPsec, \texttt{ntpd}
viene avviato soltanto quando necessario e con PTP non attivo.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
kathara exec -d "$TOPOLOGY_DIR" servergm -- bash -lc \
  "pkill chronyd 2>/dev/null || true; \
  sleep 1; \
  ptp4l -f /etc/ptp/server_gm.conf -m -i eth0 -S \
  &> /tesi_sync_lab/$RUN_DIR/ptp_server.log &"

sleep 3

kathara exec -d "$TOPOLOGY_DIR" boundary -- bash -lc \
  "ptp4l -f /etc/ptp/bc.conf -m -i eth0 -i eth1 -S \
  &> /tesi_sync_lab/$RUN_DIR/ptp_boundary.log &"

sleep 3

kathara exec -d "$TOPOLOGY_DIR" clientptp -- bash -lc \
  "ptp4l -f /etc/ptp/client_ptp.conf -m -i eth0 -S -s \
  &> /tesi_sync_lab/$RUN_DIR/ptp_client.log &"
\end{minted}
\caption{Esempio di avvio controllato dei demoni PTP.}
\label{lst:ptp_start_sequence}
\end{listing}

La sequenza di avvio è rilevante perché permette di inizializzare prima il grandmaster,
poi il boundary clock e infine il client PTP. In questo modo, il dominio PTP
viene costruito secondo la gerarchia attesa, riducendo la presenza di transitori dovuti
all'assenza iniziale di una sorgente temporale.

\subsection{Isolamento dalla rete esterna}

Nella fase di avvio si è reso necessario mantenere la connettività 
verso l'esterno per consentire l'installazione dei pacchetti necessari, 
quali \texttt{chrony}, \texttt{ntpsec} e \texttt{linuxptp}, e l'aggiornamento 
delle repository. Questo spiega la presenza di bridge temporanei verso l'esterno,
 che vengono successivamente disabilitati per garantire l'isolamento del testbed.

\begin{lstlisting}[language=bash,caption={Estratto del file \texttt{lab.conf}: bridge temporaneo verso l'esterno.},label={lst:labconf_bridged}]
servergm[bridged]=true
serverntp[bridged]=true
boundary[bridged]=true
clientchrony[bridged]=true
clientntp[bridged]=true
clientptp[bridged]=true
\end{lstlisting}

Successivamente, terminata l'inizializzazione della T2, la connettività esterna è stata
disabilitata utilizzando lo script \texttt{endInternetConnection.sh}.

Lo script disabilita l'interfaccia esterna sui nodi dotati di una sola
interfaccia interna e disabilita l'interfaccia esterna dedicata sul nodo \texttt{boundary}.
Nei nodi collegati a una sola rete interna, l'interfaccia \texttt{eth0} è
utilizzata per la topologia sperimentale, mentre \texttt{eth1} rappresenta l'interfaccia
bridged verso l'esterno. Nel nodo \texttt{boundary}, invece, \texttt{eth0} ed \texttt{eth1}
sono già utilizzate per le reti A e B; l'interfaccia esterna risulta quindi \texttt{eth2}.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
#!/bin/bash
source ./scripts/envT2.sh

echo "[+] Disattivo eth1 in tutti i nodi (post-start)"
for node in servergm serverntp clientchrony clientntp clientptp; do
  if kathara exec -d "$TOPOLOGY_DIR" "$node" 'ip link set eth1 down' >/dev/null 2>&1; then
    echo "[+] eth1 disattivata in $node"
  else
    echo "[!] eth1 non presente in $node"
  fi
done

if kathara exec -d "$TOPOLOGY_DIR" boundary 'ip link set eth2 down' >/dev/null 2>&1; then
  echo "[+] eth2 disattivata in boundary"
else
  echo "[!] eth2 non presente in boundary"
fi

echo "[DONE] Connessione Internet disattivata in tutti i nodi."
\end{minted}
\caption{Script \texttt{endInternetConnection.sh}: disabilitazione della connettività esterna.}
\label{lst:end_internet}
\end{listing}
Questo script viene eseguito in ogni run dopo l'avvio della topologia.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
kathara lclean -d "$TOPOLOGY_DIR" || true
sudo -v
printf 'n\n' | sudo -n kathara lstart -d "$TOPOLOGY_DIR" --privileged

sleep 25

./scripts/endInternetConnection.sh

sleep 5
\end{minted}
\caption{Punto del workflow in cui viene disabilitata la rete esterna.}
\label{lst:end_internet_workflow}
\end{listing}

Togliendo connettività esterna, garantiamo che disturbi non controllati possono essere
introdotti all'interno del testbed, e allo stesso modo garantiamo che i
riferimenti temporali siano interni rispetto ai nodi preventivamente stabiliti, quali
sono rispettivamente i nodi \texttt{servergm} e \texttt{serverntp}.

\section{Configurazione logica dei protocolli}
Come è stato precedentemente descritto, vi sono tre percorsi logici distinti. Le differenti
configurazioni applicate ai container nella fase di avvio, vengono
opportunamente selezionate in base all'ambiente di test selezionato.

Le tre catene logiche considerate sono:
\begin{center}
  \texttt{servergm} $\rightarrow$ \texttt{boundary} $\rightarrow$ \texttt{clientptp}
\end{center}
per PTP,
\begin{center}
  \texttt{servergm} $\rightarrow$ \texttt{clientchrony}
\end{center}
per Chrony, e
\begin{center}
  \texttt{serverntp} $\rightarrow$ \texttt{boundary} $\rightarrow$ \texttt{clientntp}
\end{center}
per NTPsec.

\subsection{Configurazione PTP}

Nella configurazione PTP, costruita tramite il software linuxptp, in particolare
tramite il demone \texttt{ptp4l}, vi è il nodo \texttt{servergm} che opera come grandmaster
interno del dominio PTP, il nodo \texttt{boundary} che rappresenta il boundary clock
del sistema tra la rete A e la rete B, ed infine si ha il \texttt{clientptp} ovvero
il nodo slave finale sulla rete B.

Nel dominio PTP si è utilizzato il dominio 24, ed in tutte le configurazioni si
è adottato il meccanismo di delay end-to-end e il timestamping software, coerente
con la strutta dei container stessi, assenti di timestamping hardware dedicato.

La configurazione del nodo \texttt{servergm} è presente nel file \texttt{server\_ptp.conf}.

\begin{lstlisting}[language=bash,caption={Configurazione PTP del nodo servergm.},label={lst:ptp-servergm}]
[global]
twoStepFlag 1
domainNumber 24
delay_mechanism E2E
time_stamping software
slaveOnly 0
priority1 128
priority2 128
[eth0]
\end{lstlisting}

Il parametro \texttt{slaveOnly 0} rende possibile assegnare al nodo il ruolo di
master. I valori \texttt{priority1} e \texttt{priority2}, entrambi pari a 128, così
opportunamente scelti per \texttt{servergm}, permettono al nodo di avere prelazione
nella scelta come di selezione dei master. Così facendo si ha che la gerarchia
PTP viene resa coerente con il disegno logico sperimentale nel quale il \texttt{servergm}
rappresenta il riferimento temporale principale del dominio.

Differentemente si ha che il nodo \texttt{boundary} è configurato attraverso un
interfaccia multipla. È connesso alla rete A tramite \texttt{eth0} e alla rete B
tramite \texttt{eth1}. La configurazione è contenuta nel file \texttt{boundary\_ptp.conf}.

\begin{lstlisting}[language=bash,caption={Configurazione PTP del nodo boundary.},label={lst:ptp-boundary}]
[global]
twoStepFlag 1
domainNumber 24
delay_mechanism E2E
time_stamping software
slaveOnly 0
priority1 200
priority2 200
announceReceiptTimeout 3
tx_timestamp_timeout 20
[eth0]
[eth1]
\end{lstlisting}

Allo stesso modo del servergm anceh il \texttt{boundary} ha una configurazione di
tipo \texttt{slaveOnly 0}, dovendo svolgere il ruolo di master sulla porta downstream
verso \texttt{clientptp}. Tuttavia, i valori \texttt{priority1} e \texttt{priority2}
sono pari a 200, riducendo la preferenza rispetto al \texttt{servergm}. Questa configurazione
permette al boundary clock di sincronizzarsi rispetto al grandmaster sulla rete A
e di propagare la sincronizzazione verso la rete B.

Il nodo \texttt{clientptp} è configurato invece come slave PTP. La configurazione
è contenuta nel file \texttt{client\_ptp.conf}.

\begin{lstlisting}[language=bash,caption={Configurazione PTP del nodo clientptp.},label={lst:ptp-client}]
[global]
twoStepFlag 1
domainNumber 24
delay_mechanism E2E
time_stamping software
summary_interval 1
logging_level 6
slaveOnly 1
announceReceiptTimeout 3
tx_timestamp_timeout 20
verbose 1
logMinDelayReqInterval 0
logAnnounceInterval 1
logSyncInterval 0
[eth0]
\end{lstlisting}

Impostando il parametro \texttt{slaveOnly 1} si impedisce al client di
candidarsi come master, vincolandolo alla sincronizzazione rispetto al nodo upstream
selezionato nel dominio PTP. Per quanto riguarda i parametri rivolti agli
intervalli dei messaggi e al livello di logging, sono stati opportunamente modificati
per ottenere un comportamento osservabile più coerente con le necessità dell'analisi dati.

\subsection{Configurazione Chrony}

Nella configurazione Chrony il nodo \texttt{servergm} svolge il ruolo di
sorgente temporale interna, mentre \texttt{clientchrony} opera come client
Chrony.

La configurazione del server Chrony è contenuta nel file \texttt{server\_chrony.conf}.

\begin{lstlisting}[language=bash,caption={Configurazione Chrony del nodo servergm.},label={lst:chrony-server}]
server 127.127.1.0
local stratum 8
manual
makestep 1 -1
driftfile /var/lib/chrony/chrony.drift
bindaddress 0.0.0.0
allow 10.0.10.0/24
log measurements statistics tracking refclocks
logchange 0.01
\end{lstlisting}

La specifica \texttt{local stratum 8} permette al nodo di comportarsi come sorgente
temporale interna indipendentemente dalla presenza di un riferimento esterno. La
direttiva \texttt{allow 10.0.10.0/24} autorizza i nodi della rete A a interrogare
il server Chrony. La specifica \texttt{bindaddress 0.0.0.0} abilita l'ascolto sulle
interfacce disponibili del container.

\begin{sloppypar}
  Il client Chrony è configurato per utilizzare come sorgente il nodo \texttt{servergm},
  raggiungibile all'indirizzo \texttt{10.0.10.1}. La configurazione è presente nel file
  \texttt{client\_chrony.conf}.
\end{sloppypar}

\begin{lstlisting}[language=bash,caption={Configurazione Chrony del nodo clientchrony.},label={lst:chrony-client}]
server 10.0.10.1 iburst minpoll 1 maxpoll 2
driftfile /var/lib/chrony/chrony.drift
logdir /analysis/raw_logs/T2/chrony_logs
log measurements statistics tracking refclocks
logchange 0.01
\end{lstlisting}

L'opzione di \texttt{iburst} permette al client del dominio di acquisire più
rapidamente la sorgente nella fase iniziale. I parametri \texttt{minpoll 1} e
\texttt{maxpoll 2} limitano l'intervallo di polling, mantenendo una frequenza di
campionamento elevata rispetto alla durata delle run sperimentali. La direttiva \texttt{driftfile}
specifica il file utilizzato da Chrony per memorizzare la stima del drift del
clock locale.

\subsection{Configurazione NTPsec}

Nella configurazione NTPsec, realizzata tramite \texttt{ntpd}, si ha come
riferimento temporale il nodo \texttt{serverntp}, utilizzando il clock locale. Il
nodo \texttt{boundary} si sincronizza con \texttt{serverntp} sulla rete A e rende
disponibile il servizio NTPsec verso la rete B verso il nodo \texttt{clientntp}, collocato
sulla rete B.

La catena logica è:

\begin{center}
  \texttt{serverntp} $\rightarrow$ \texttt{boundary} $\rightarrow$ \texttt{clientntp}
\end{center}

La configurazione è presente nel file \texttt{server\_ntp.conf}.

\begin{lstlisting}[language=bash,caption={Configurazione NTPsec del nodo serverntp.},label={lst:ntpsec-server}]
server 127.127.1.0
fudge 127.127.1.0 stratum 8
local stratum 8

driftfile /var/lib/ntpsec/ntp.drift

restrict default kod nomodify notrap nopeer limited
restrict 127.0.0.1
restrict ::1
restrict 10.0.10.0 mask 255.255.255.0 nomodify notrap
\end{lstlisting}

La sorgente \texttt{127.127.1.0} indica l'utilizzo del clock locale. Lo stratum
è impostato a 8, ciò è coerente con la sorgente temporale interna. La direttiva
\texttt{driftfile} specifica il file in cui NTPsec memorizza la stima del drift del
clock. Le direttive \texttt{restrict} limitano le operazioni consentite ai client
e autorizzano la rete \texttt{10.0.10.0/24} a interrogare il server.

La configurazione è contenuta nel file \texttt{boundary\_ntp.conf}.

\begin{lstlisting}[language=bash,caption={Configurazione NTPsec del nodo boundary.},label={lst:ntpsec-boundary}]
server 10.0.10.4 iburst
driftfile /var/lib/ntpsec/ntp.drift

restrict default kod nomodify notrap nopeer limited
restrict 10.0.10.0 mask 255.255.255.0 nomodify notrap
restrict 10.0.20.0 mask 255.255.255.0
restrict 127.0.0.1
restrict ::1
\end{lstlisting}

La direttiva \texttt{server 10.0.10.4 iburst} indica che il boundary si sincronizza
con \texttt{serverntp} sulla rete A. La rete B è autorizzata tramite la direttiva
\texttt{restrict 10.0.20.0 mask 255.255.255.0}, permettendo a \texttt{clientntp}
di utilizzare il boundary come sorgente NTPsec.

Il client NTPsec utilizza il boundary sulla rete B, raggiungibile all'indirizzo \texttt{10.0.20.1}.
La configurazione è contenuta nel file \texttt{client\_ntp.conf}.

\begin{lstlisting}[language=bash,caption={Configurazione NTPsec del nodo clientntp.},label={lst:ntpsec-client}]
server 10.0.20.1 iburst
driftfile /var/lib/ntpsec/ntp.drift
restrict 127.0.0.1
restrict ::1
\end{lstlisting}

Differentemente dalla configurazione PTP, il nodo \texttt{boundary} opera come classico
nodo intermedio e non come boundary clock nel senso del protocollo IEEE 1588.

\section{Emulazione degli impairment di rete}

Per la valutazione del comportamento adottato dai demoni di sincronizzazione in condizioni
non ideali, sono stati introdotti impairment di rete controllati attraverso l'ausilio
di Linux Traffic Control. La topologia di rete è rimasta invariata in tutte le run,
garantendo omogeneità, ma allo stesso tempo sono cambiati i parametri degli impairment
stessi.

TC, ovvero Linux Traffic Control, permette di controllare, classificare, modellare
e pianificare il traffico di rete. Si dispone di discipline di accodamento (qdisc)
e filtri per gestire e manipolare la trasmissione dei pacchetti, inclusa la modifica
del loro contenuto. \cite{Chapter32Linux}. Nel caso specifico è stato utilizzata una
combinazione di \texttt{netem} e \texttt{tbf}. La prima qdisc (CLASSLESS QDISC), cioè
Network Emulator (netem) è un miglioramento delle funzionalità di controllo del traffico
di Linux che consente di aggiungere ritardo, perdita di pacchetti, duplicazione e
altre caratteristiche ai pacchetti in uscita da un'interfaccia di rete selezionata.
\cite{Tc8LinuxManual}

Invece la seconda (CLASSLESS QDISC), cioè tbf, è il filtro Token Bucket che è adatto
a rallentare il traffico a una velocità configurata con precisione, quindi
limitare la bandwidth . \cite{Tc8LinuxManual}

Gli scenari sono stati definiti in modo parametrico mediante file di configurazione
separati dal codice di esecuzione.

\subsection{Linux Traffic Control}

Linux Traffic Control, viene richiamato tramite il comando \texttt{tc}.

Le due qdisc sono state combinate, la prima qdisc \texttt{netem} è stata
applicata come root. Diversamente la seconda è stata attaccata come qdisc figlia
della prima. La struttura logica si configura come segue: qdisc root di tipo \texttt{netem}
con handle \texttt{1:}, e qdisc figlia di tipo \texttt{tbf} con handle \texttt{10:}.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
tc qdisc replace dev $IFACE root handle 1: netem \
  delay $DELAY $JITTER \
  loss $LOSS \
  reorder $REORDER

tc qdisc add dev $IFACE parent 1: handle 10: tbf \
  rate $RATE \
  burst 32kbit \
  latency 400ms
\end{minted}
\caption{Schema generale di applicazione di netem e tbf.}
\label{lst:tc-netem-tbf-generale}
\end{listing}

\texttt{tc qdisc replace} è un comando utilizzato per sostituire o per creare in
caso non esistesse la qdisc root sull'interfaccia. Così facendo, l'applicazione
dello scenario risulta idempotente rispetto alla configurazione della qdisc
principale.

\texttt{tc qdisc add} è il comando adottato all'aggiunta della qdisc figlia alla root,
agganciandola al parent \texttt{1:}, riuscendo così ad introdurre sia
perturbazioni temporali e probabilistiche, sia una limitazione del rate.

Per avere un quadro più chiaro, nei file del testbed sono presenti 
anche due script di supporto, \texttt{apply\_netem.sh} e \texttt{cleanup\_netem.sh}, i
quali però non sono utilizzati nel flusso principale del testbed, ma hanno solo funzione
di debugging. Il primo è stato pensato per applicare singoli disturbi su un'interfaccia
specifica, mentre il secondo consente di rimuovere manualmente la qdisc root da un'interfaccia.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
tc qdisc del dev <interface> root
\end{minted}
\caption{Rimozione manuale di una qdisc da un'interfaccia.}
\label{lst:tc-cleanup}
\end{listing}

Necessitando però l'aggiunta simultanea di più parametri, gli script di supporto non
vengono quindi gestiti direttamente dallo script principale. Lo stesso avviene per
la rimozione in quanto andando a chiudere i container ogni run, le qdisc vengono automaticamente
rimosse.

\subsection{Parametri di degrado}

I file \texttt{.conf} contengono le variabili che definiscono i parametri di degrado,
sono presenti nella directory \texttt{scripts/netem/scenarios/} e vengono importati
dallo script principale tramite \texttt{source}. In ogni file vi sono variabili Bash
che specificano gli esatti parametri da passare ai comandi \texttt{tc}. La struttura
della directory degli scenari è la seguente:

\begin{lstlisting}[caption={Organizzazione dei file di scenario.},label={lst:scenario-directory}]
scripts/netem/
|-- scenarios/
|   |-- low.conf
|   |-- medium.conf
|   |-- high.conf
|   `-- scenario_matrix.md
|-- apply_netem.sh
|-- cleanup_netem.sh
`-- bootstrapT3_V2.sh
\end{lstlisting}

Nella seguente tabella vengono riportati i parametri utilizzati~\ref{tab:parametri-degrado}.

\begin{table}[H]
  \centering
  \begin{tabular}{lll}
    \hline
    \textbf{Parametro} & \textbf{Significato}     & \textbf{Uso nel testbed}    \\
    \hline
    \texttt{DELAY}     & Ritardo medio introdotto & Parametro di \texttt{netem} \\
    \texttt{JITTER}    & Variabilità del ritardo  & Parametro di \texttt{netem} \\
    \texttt{LOSS}      & Probabilità di perdita   & Parametro di \texttt{netem} \\
    \texttt{REORDER}   & Probabilità di riordino  & Parametro di \texttt{netem} \\
    \texttt{RATE}      & Limite di banda          & Parametro di \texttt{tbf}   \\
    \hline
  \end{tabular}
  \caption{Parametri utilizzati per definire gli scenari di degrado.}
  \label{tab:parametri-degrado}
\end{table}

Un esempio di file di scenario è rappresentato da \texttt{low.conf}:

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
DELAY="5ms"
JITTER="1ms"
LOSS="0.1%"
REORDER="1%"
RATE="50Mbit"
\end{minted}
\caption{Esempio di file di scenario low.conf.}
\label{lst:low-conf}
\end{listing}

È importante notare che i valori presenti nel file di configurazione sono parte della
sintassi interpretata da \texttt{tc}. Si ha quindi che i valori temporali sono espressi
in millisecondi, le probabilità in percentuale e il limite di banda in unità
compatibili con il comando \texttt{tbf}.

La logica, successivamente descritta nel paragrafo \ref{cha:AnalisiSperimentale},
distingue tre scenari (livelli) di degrado progressivo, identificati come
\texttt{low}, \texttt{medium} e \texttt{high}.

\subsection{Applicazione degli scenari}

Per quanto riguarda l'applicazione degli impairment, si è deciso di degradare il
tratto di rete tra il nodo boundary e il client finale, sia per PTP che per NTPsec.
Questo asseconda la logica di \texttt{qdisc} che si applica sul traffico in uscita
da un'interfaccia di rete, cioè in egress. Nei due scenari quindi, si è
applicato gli impairment sull'interfaccia \texttt{eth1} del nodo \texttt{boundary}.
Tale interfaccia collega il boundary alla rete B e rappresenta quindi il ramo downstream
verso i client \texttt{clientptp} e \texttt{clientntp}. In questo modo, la sorgente
temporale sulla rete A rimane non perturbata, mentre viene degradato il
collegamento verso il client finale.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
NETNS="boundary"
IFACE="eth1"   # boundary lato rete B
\end{minted}
\caption{Selezione dell'interfaccia degradata per PTP e NTPsec.}
\label{lst:ptp-ntpsec-iface}
\end{listing}

L'applicazione effettiva della qdisc su \texttt{boundary:eth1} avviene tramite
\texttt{kathara exec}, eseguendo il comando \texttt{tc} all'interno del container
del nodo \texttt{boundary}.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
kathara exec -d "$TOPOLOGY_DIR" $NETNS -- bash -lc \
  "tc qdisc replace dev $IFACE root handle 1: netem \
      delay $DELAY $JITTER \
      loss $LOSS \
      reorder $REORDER"

kathara exec -d "$TOPOLOGY_DIR" $NETNS -- bash -lc \
  "tc qdisc add dev $IFACE parent 1: handle 10: tbf \
      rate $RATE \
      burst 32kbit \
      latency 400ms"
\end{minted}
\caption{Applicazione di netem e tbf su boundary:eth1.}
\label{lst:tc-boundary-eth1}
\end{listing}

In PTP è quindi possibile osservare l'effetto del degrado sul ramo downstream del
dominio PTP, distinguendo il comportamento del boundary clock da quello del
client finale.

Analogamente anche in NTPsec, poichè il \texttt{clientntp} riceve dal nodo
\texttt{boundary} la sincronizzazione attraverso il collegamento degradato, è
possibile valutare l'effetto dell'hop intermedio e del degrado downstream sulla
qualità della sincronizzazione del client.

Per Chrony, invece, la catena logica è diretta tra \texttt{servergm} e \texttt{clientchrony},
entrambi collocati sulla rete A. Per questo motivo sono state considerate due
modalità distinte di applicazione dello scenario: una con qdisc applicata sull'interfaccia
\texttt{eth0} di \texttt{clientchrony} e una con qdisc applicata sull'interfaccia
\texttt{eth0} di \texttt{servergm}.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
CLIENT_IFACE="eth0"

kathara exec -d "$TOPOLOGY_DIR" clientchrony -- bash -lc \
  "tc qdisc replace dev $CLIENT_IFACE root handle 1: netem \
      delay $DELAY $JITTER loss $LOSS reorder $REORDER"

kathara exec -d "$TOPOLOGY_DIR" clientchrony -- bash -lc \
  "tc qdisc add dev $CLIENT_IFACE parent 1: handle 10: tbf \
      rate $RATE burst 32kbit latency 400ms"
\end{minted}
\caption{Applicazione di netem e tbf su clientchrony:eth0.}
\label{lst:tc-clientchrony}
\end{listing}

La seconda modalità applica gli stessi parametri all'interfaccia \texttt{eth0} di
\texttt{servergm}.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
CLIENT_IFACE="eth0"

kathara exec -d "$TOPOLOGY_DIR" servergm -- bash -lc \
  "tc qdisc replace dev $CLIENT_IFACE root handle 1: netem \
      delay $DELAY $JITTER loss $LOSS reorder $REORDER"

kathara exec -d "$TOPOLOGY_DIR" servergm -- bash -lc \
  "tc qdisc add dev $CLIENT_IFACE parent 1: handle 10: tbf \
      rate $RATE burst 32kbit latency 400ms"
\end{minted}
\caption{Applicazione di netem e tbf su servergm:eth0.}
\label{lst:tc-servergm}
\end{listing}

Questa scelta, come poi evidenziato nella fase di analisi dei dati
\ref{cha:AnalisiSperimentale}, serve a mettere in evidenza l'effetto della perturbazione
sul nodo che riceve la sincronizzazione rispetto al nodo che svolge il ruolo di sorgente
temporale interna.

\section{Automazione e raccolta dei log}

\begin{sloppypar}
  Il flusso logico di esecuzione del testbed è realizzato con uno script Bash
  principale, denominato \texttt{bootstrapT3\_V2.sh}. Coordina l'intera ciclo di funzionalità
  precedentemente descritte: dalla pulizia/creazione della topologia, alla logica dell'applicazione
  degli impairment, fino alla raccolta dati, garantendo ripetibilità e separazione funzionale
  tra le run sperimentali.
\end{sloppypar}

Così facendo, sì è ridotto al minimo l'intervento manuale, evitando errori di alcun
tipo, e soprattutto si è garantito l'assenza di interferenza tra processi residui o
configurazioni differenti, semplificando anche quella che poi è stata l'analisi dati
\ref{cha:AnalisiSperimentale}.

\subsection{Script di esecuzione}

Nello script vengono definiti gli scenari da eseguire (evidenziata dalla variabile
\texttt{SCENARIOS}), le directory di output dei log (evidenziata dalla variabile \texttt{RAWLOG\_ROOT}),
la topologia (evidenziata dalla variabile \texttt{TOPOLOGY\_DIR}), e la
numerazione dei run (evidenziata dalla variabile \texttt{RUNS}).

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
SCENARIOS=("low" "medium" "high")
SCENARIO_DIR="$(dirname "$0")/scenarios"
RAWLOG_ROOT="analysis/raw_logs/T3_multiplerun"
NETNS="boundary"
IFACE="eth1"   # boundary lato rete B
TOPOLOGY_DIR="topologies/T2"

RUNS=15

mkdir -p "$RAWLOG_ROOT"
\end{minted}
\caption{Variabili globali dello script di automazione.}
\label{lst:auto-vars}
\end{listing}

La struttura è basata su funzioni separate per i protocolli PTP, NTPsec e Chrony,
ognuna opportunamente implementata, con la specifica applicata nel caso di Chrony,
distinguendo il caso in cui l'impairment è applicato su \texttt{servergm} dal caso
in cui è applicato su \texttt{clientchrony}.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
run_ptp_scenario() {
    ...
}

run_ntpsec_scenario() {
    ...
}

run_chrony_scenario_servergm() {
    ...
}

run_chrony_scenario_clientchrony() {
    ...
}
\end{minted}
\caption{Funzioni principali dello script di automazione.}
\label{lst:auto-functions}
\end{listing}

Le funzioni sono richiamate in un ciclo annidato, iterando prima sugli scenari e
poi sulle run per ogni scenario, fetchate degli opportuni parametri. A partire
da questi valori viene costruita una directory specifica in cui salvare i log
della run. Un esempio:

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
local S="$1"
local RUN_ID="$2"
local RUN_DIR="$RAWLOG_ROOT/ptp/$S/run$(printf "%02d" "$RUN_ID")"

mkdir -p "$RUN_DIR"
\end{minted}
\caption{Creazione della directory di output per una run PTP.}
\label{lst:auto-run-dir-ptp}
\end{listing}

All'inizio di ogni run viene importato il file di scenario corrispondente
tramite \texttt{source}. In questo modo le variabili \texttt{DELAY}, \texttt{JITTER},
\texttt{LOSS}, \texttt{REORDER} e \texttt{RATE} diventano disponibili nello
shell corrente e possono essere passate direttamente ai comandi \texttt{tc}.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
source "$SCENARIO_DIR/$S.conf"

echo "[DEBUG] delay=$DELAY jitter=$JITTER loss=$LOSS reorder=$REORDER rate=$RATE"
\end{minted}
\caption{Importazione dei parametri dello scenario corrente.}
\label{lst:auto-source-scenario}
\end{listing}

Pulizia dei container residui e avvio topologia.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
kathara lclean -d "$TOPOLOGY_DIR" || true

sudo -v
printf 'n\n' | sudo -n kathara lstart -d "$TOPOLOGY_DIR" --privileged

sleep 25

./scripts/endInternetConnection.sh

sleep 5
\end{minted}
\caption{Pulizia e avvio della topologia prima di una run.}
\label{lst:auto-lstart}
\end{listing}

I comandi di sleep presenti sono stati inseriti al fine di garantire la stabilizzazione
della topologia e dei processi prima di procedere con il resto.

Esempio di avvio processi PTP, con avvio container e implementazione impairment
di rete.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
kathara exec -d "$TOPOLOGY_DIR" servergm -- bash -lc \
  "pkill chronyd 2>/dev/null || true; \
  sleep 1; \
  ptp4l -f /etc/ptp/server_gm.conf -m -i eth0 -S \
  &> /tesi_sync_lab/$RUN_DIR/ptp_server.log &"

sleep 3

kathara exec -d "$TOPOLOGY_DIR" boundary -- bash -lc \
  "ptp4l -f /etc/ptp/bc.conf -m -i eth0 -i eth1 -S \
  &> /tesi_sync_lab/$RUN_DIR/ptp_boundary.log &"

sleep 3

kathara exec -d "$TOPOLOGY_DIR" clientptp -- bash -lc \
  "ptp4l -f /etc/ptp/client_ptp.conf -m -i eth0 -S -s \
  &> /tesi_sync_lab/$RUN_DIR/ptp_client.log &"
\end{minted}
\caption{Avvio dei processi PTP nella run automatizzata.}
\label{lst:auto-ptp-start}
\end{listing}

\begin{sloppypar}
  I log standard prodotti da \texttt{ptp4l} vengono rediretti nei file \texttt{ptp\_server.log},
  \texttt{ptp\_boundary.log} e \texttt{ptp\_client.log}. Dopo l'avvio dei processi,
  lo script attende una fase di stabilizzazione prima di chiudere la topologia.
\end{sloppypar}

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
echo "[+] Stabilizzazione PTP..."
sleep 250

kathara lclean -d "$TOPOLOGY_DIR"
\end{minted}
\caption{Fase di stabilizzazione PTP e chiusura della run.}
\label{lst:auto-ptp-sleep}
\end{listing}

Analogamente avviene per NTPsec, con opportune modifiche nel merito del demone e
della raccolta dati tramite \texttt{ntpq -p}.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
kathara exec -d "$TOPOLOGY_DIR" boundary -- bash -lc \
  'pkill ntpd 2>/dev/null; sleep 2; /usr/sbin/ntpd -g -c /etc/ntpsec/ntp.conf &'
\end{minted}
\caption{Avvio controllato di NTPsec sul boundary.}
\label{lst:auto-ntpsec-start}
\end{listing}

La raccolta dei dati NTPsec viene effettuata tramite \texttt{ntpq -p}, eseguito
periodicamente ogni 15 secondi. Il comando viene eseguito sia sul client sia sul boundary,
così da osservare separatamente il comportamento del nodo intermedio e quello
del client finale.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
kathara exec -d "$TOPOLOGY_DIR" clientntp -- bash -lc "
  nohup bash -c '
    while true; do
      echo \"--- \$(date +%T) ---\"
      ntpq -p
      sleep 15
    done
  ' > /tesi_sync_lab/$RUN_DIR/ntp_client_live.log 2>&1 &
"
\end{minted}
\caption{Loop di logging NTPsec sul client.}
\label{lst:auto-ntpsec-client-loop}
\end{listing}

Un loop analogo viene eseguito sul nodo \texttt{boundary}, salvando l'output nel file
\texttt{ntp\_boundary\_live.log}. La durata della run NTPsec è più lunga rispetto
a quella PTP, poiché il protocollo può richiedere più tempo per l'inizializzazione,
la selezione della sorgente e la stabilizzazione delle metriche osservabili tramite
\texttt{ntpq}.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
sleep 1500

kathara lclean -d "$TOPOLOGY_DIR"
\end{minted}
\caption{Durata della fase di raccolta NTPsec.}
\label{lst:auto-ntpsec-sleep}
\end{listing}

\begin{sloppypar}
  In Chrony la funzione che applica il degrado su \texttt{servergm} costruisce la
  directory di output nella gerarchia \texttt{chrony\_servergm}. La funzione che
  applica il degrado su \texttt{clientchrony} utilizza invece la gerarchia \texttt{chrony\_clientchrony}.
  Questa distinzione consente di mantenere separati i dati delle due modalità sperimentali.
\end{sloppypar}

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
RUN_DIR="$RAWLOG_ROOT/chrony_servergm/$S/run$(printf "%02d" "$RUN_ID")"

RUN_DIR="$RAWLOG_ROOT/chrony_clientchrony/$S/run$(printf "%02d" "$RUN_ID")"
\end{minted}
\caption{Directory di output per le due modalità Chrony.}
\label{lst:auto-chrony-dirs}
\end{listing}

Prima della raccolta, lo script salva lo stato effettivo della qdisc applicata.
Questo file consente di verificare a posteriori che lo scenario sia stato
effettivamente applicato all'interfaccia prevista.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
kathara exec -d "$TOPOLOGY_DIR" clientchrony -- bash -lc \
  "tc -s qdisc show dev $CLIENT_IFACE" \
  > "$RUN_DIR/netem_state.txt"
\end{minted}
\caption{Salvataggio dello stato della qdisc in una run Chrony.}
\label{lst:auto-chrony-qdisc-state}
\end{listing}

Dopo l'applicazione della qdisc, lo script attende una breve fase di convergenza
e inizializza i file destinati alla raccolta delle serie temporali.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
SAMPLES=20
INTERVAL=60

TRACK_FILE="$RUN_DIR/chrony_tracking_series.txt"
SOURCES_FILE="$RUN_DIR/chrony_sources_series.txt"
STATS_FILE="$RUN_DIR/chrony_sourcestats_series.txt"
ACT_FILE="$RUN_DIR/chrony_activity_series.txt"

: > "$TRACK_FILE"
: > "$SOURCES_FILE"
: > "$STATS_FILE"
: > "$ACT_FILE"
\end{minted}
\caption{Inizializzazione dei file di output Chrony.}
\label{lst:auto-chrony-files}
\end{listing}

Nel caso della funzione con impairment su \texttt{clientchrony}, lo script raccoglie
venti campioni a intervalli di 60 secondi. Nella funzione con impairment su
\texttt{servergm}, la struttura è analoga, ma il numero di campioni è impostato a
dieci. Ogni campione viene marcato temporalmente tramite \texttt{date -Is}, in
modo da mantenere un riferimento temporale testuale per la successiva analisi.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos,
  autogobble
]{bash}
for i in $(seq 1 $SAMPLES); do
  ts="$(date -Is)"
  echo "===== SAMPLE $i/$SAMPLES @ $ts =====" | tee -a "$TRACK_FILE" "$SOURCES_FILE" "$STATS_FILE" "$ACT_FILE" >/dev/null

  kathara exec -d "$TOPOLOGY_DIR" clientchrony "chronyc tracking" \
    >> "$TRACK_FILE" 2>&1
  echo "" >> "$TRACK_FILE"

  kathara exec -d "$TOPOLOGY_DIR" clientchrony "chronyc sources -v" \
    >> "$SOURCES_FILE" 2>&1
  echo "" >> "$SOURCES_FILE"

  kathara exec -d "$TOPOLOGY_DIR" clientchrony "chronyc sourcestats" \
    >> "$STATS_FILE" 2>&1
  echo "" >> "$STATS_FILE"

  kathara exec -d "$TOPOLOGY_DIR" clientchrony "chronyc activity" \
    >> "$ACT_FILE" 2>&1
  echo "" >> "$ACT_FILE"

  sleep "$INTERVAL"
done
\end{minted}
\caption{Raccolta periodica delle metriche Chrony.}
\label{lst:auto-chrony-loop}
\end{listing}
Nella parte del main Bash, si ha un'esecuzione sistematica di tutte le
combinazioni di scenario e run.

\begin{listing}[H]
\begin{minted}[
  frame=single,
  fontsize=\small,
  breaklines,
  linenos
]{bash}
for S in "${SCENARIOS[@]}"; do
    for RUN_ID in $(seq 1 $RUNS); do
        run_ptp_scenario "$S" "$RUN_ID"
    done
done

for S in "${SCENARIOS[@]}"; do
    for RUN_ID in $(seq 1 $RUNS); do
        run_ntpsec_scenario "$S" "$RUN_ID"
    done
done

for S in "${SCENARIOS[@]}"; do
    for RUN_ID in $(seq 1 $RUNS); do
        run_chrony_scenario_servergm "$S" "$RUN_ID"
    done
done

for S in "${SCENARIOS[@]}"; do
    for RUN_ID in $(seq 1 $RUNS); do
        run_chrony_scenario_clientchrony "$S" "$RUN_ID"
    done
done

echo "[OK] Tutti i test T3 completati."
\end{minted}
\caption{Ciclo finale di esecuzione delle run.}
\label{lst:auto-final-loop}
\end{listing}

\subsection{Organizzazione dei dati raccolti}

I log sono salvati nella root directory \texttt{analysis/raw\_logs/T3\_multiplerun}
con la seguente struttura:

\begin{lstlisting}[caption={Organizzazione generale dei log sperimentali.},label={lst:auto-log-tree}]
analysis/raw_logs/T3_multiplerun/
|-- ptp/
|   |-- low/
|   |-- medium/
|   `-- high/
|-- ntpsec/
|   |-- low/
|   |-- medium/
|   `-- high/
|-- chrony_servergm/
|   |-- low/
|   |-- medium/
|   `-- high/
`-- chrony_clientchrony/
    |-- low/
    |-- medium/
    `-- high/
\end{lstlisting}

\begin{lstlisting}[caption={Esempio di percorso per una run PTP.},label={lst:auto-ptp-path}]
analysis/raw_logs/T3_multiplerun/ptp/low/run01/
\end{lstlisting}

Nella directory PTP i file prodotti sono lo stato della qdisc e i tre log dei processi.

\begin{lstlisting}[caption={File prodotti da una run PTP.},label={lst:auto-ptp-files}]
ptp_server.log
ptp_boundary.log
ptp_client.log
netem_state.txt
\end{lstlisting}

Per NTPsec, i file principali sono i log ottenuti tramite interrogazione periodica
di \texttt{ntpq -p} sul client e sul boundary, oltre allo stato della qdisc.

\begin{lstlisting}[caption={File prodotti da una run NTPsec.},label={lst:auto-ntpsec-files}]
ntp_client_live.log
ntp_boundary_live.log
netem_state.txt
\end{lstlisting}

Per Chrony, i dati sono organizzati in quattro serie testuali, corrispondenti ai
comandi \texttt{chronyc} utilizzati durante la raccolta.

\begin{lstlisting}[caption={File prodotti da una run Chrony.},label={lst:auto-chrony-files-list}]
chrony_tracking_series.txt
chrony_sources_series.txt
chrony_sourcestats_series.txt
chrony_activity_series.txt
netem_state.txt
\end{lstlisting}

Il file \texttt{chrony\_tracking\_series.txt} contiene lo stato del tracking del client Chrony. 
\newline
Il file \texttt{chrony\_sources\_series.txt} riporta le sorgenti viste dal client e il loro stato. 
\newline
Il file \texttt{chrony\_sourcestats\_series.txt} contiene le statistiche associate alla sorgente.
\newline
\texttt{chrony\_activity\_series.txt}descrive lo stato di attività delle sorgenti.

Cosi facendo si è riusciti a ottenere una riconducibilità diretta tra i dati e i protocolli
da cui essi sono dipesi.